% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designs.R
\name{make_design}
\alias{make_design}
\title{Build a design, optionally with redesigned parameters}
\usage{
make_design(
  design = c("pate_with_sampling", "two_arm_trial", "two_arm_with_blocks",
    "logit_probit_ols", "two_arm_trial_rdss", "two_arm_with_blocks_rdss", "4.1", "11.5",
    "2.1", "2.2"),
  ...
)
}
\arguments{
\item{design}{Design id or book alias. Defaults to \code{"two_arm_trial"} (or the
first installed design). Tab-completion offers the full library list.}

\item{...}{Named parameter values passed to \code{redesign()}.}
}
\value{
A design object (or a list of designs if a parameter is a vector
and \code{redesign()} expands).
}
\description{
Loads the declared design at its defaults. Named \code{...} values are applied
with \code{\link[DeclareDesignZero:redesign]{DeclareDesignZero::redesign()}}. The design object is the source of
truth for which names can be changed; see \code{\link[=get_args]{get_args()}}.
}
\details{
In RStudio, Positron, and other tools that complete from formals, typing
\verb{make_design("} and pressing Tab lists installed design ids (and aliases).
See \code{\link[=list_designs]{list_designs()}} for the same catalogue in the console.
}
\examples{
\dontrun{
make_design()
make_design("two_arm_trial", b = 0.5)
make_design("2.1", b = 0.5)  # book alias
}
}
